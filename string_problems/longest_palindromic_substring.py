class Solution:
    def longestPalindrome(self, s: str) -> str:
        #
        # 1. get two indexes on head and tail
        # 2. while abs(i,j) > 1
        # 3. if str[i] != str[j]
        #


# input: "babad"
# expected: "aba"
